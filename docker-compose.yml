version: '3'
services:
  app:
    container_name: dashboard
    restart: always
    build: .
    ports:
      - '80:3800'
    links:
      - mongo
    environment:
      - NODE_ENV=production
  mongo:
    container_name: mongo
    image: mongo
    #ports:
    #  - "27017:27017"

      
